---
const { frontmatter, active } = Astro.props
---

<div class={`carousel-item ${active ? "active" : ""}`}>
  <a href={`/boletines/boletin-${frontmatter.index}`}>
    
    <img src={frontmatter.img} class="d-block w-100" alt="..." />
    
    <div class="carousel-caption d-none d-md-block">
      <h2>{frontmatter.short}</h2>
      <p class="fs-4">
        Boletín {frontmatter.index}, {frontmatter.mes} {frontmatter.vigencia}
      </p>
    </div>

  </a>
</div>

<style>
  p, h2 {
    text-shadow: #FC0 1px 0 10px;
  }
</style>